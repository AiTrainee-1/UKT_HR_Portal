# Dev server
PORT=5173
BASE_PATH=/

# Proxy /api to Django (default). Leave VITE_API_URL empty to use the proxy.
VITE_API_PROXY_TARGET=http://localhost:8080

# Optional: call API directly (skip proxy). Example: http://localhost:8080
# VITE_API_URL=
